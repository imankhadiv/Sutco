= simple_form_for(@training_attendance) do |f|
  = f.error_notification

  .form-inputs
    = f.input :username
    = f.input :recorder
    = f.association :training

  .form-actions
    = f.button :submit
