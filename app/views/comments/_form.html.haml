-#= simple_form_for([@board, @conversation, @comment]) do |f|
-#  = f.error_notification
-#
-#  = f.text_area :body, :rows => 3, :cols => 70, :placeholder => 'Enter Comment'
-#  %p
-#  .form-actions
-#    = f.button :submit
