%h1 My message boards

%table.table.table-striped.table-nonfluid.table-hover
  %tr
    %th Boards
    %th Conversations

  - @boards.each do |board|
    %tr
      -if board.authorized(current_user)
        %td= link_to board.title, board
        %td= board.conversations.count

%br

-#= link_to 'New Board', new_board_path
