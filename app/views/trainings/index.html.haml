.page-header
  %h1 List of trainings

%table.table.table-striped.table-responsive.table-nonfluid.table-customstyle.table-hover
  %thead
    %tr
      %th Title
      %th Type
      %th Date
      %th Time
      %th Duration
      - if ((current_user.role? "SeniorCommittee") || (current_user.role? "TechManager"))
        %th{colspan: "3"} Options
      - else
        %th Options
  %tbody
    - @trainings.each do |training|
      %tr
        %td= training.title
        %td= training.category
        %td= training.date.strftime("%B %d, %Y")
        %td= training.time.strftime("%I:%M%p")
        %td  #{training.duration} minutes
        %td= link_to 'View', training
        - if ((current_user.role? "SeniorCommittee") || (current_user.role? "TechManager"))
          %td= link_to 'Edit', edit_training_path(training)
          %td= link_to 'Delete', training, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

- if ((current_user.role? "SeniorCommittee") || (current_user.role? "TechManager"))
  %a.btn.btn-warning.btn-lg{href: new_training_path, role: "button"} Create New Training

  %br
  %br