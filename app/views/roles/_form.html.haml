= simple_form_for(@role) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :position
    = f.input :required_number
    = f.input :available_number
    = f.association :show

  .form-actions
    = f.button :submit
